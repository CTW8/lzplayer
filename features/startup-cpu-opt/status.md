# startup-cpu-opt 进度

> 最后更新: 2026-08-08
> 总体状态: Doing

## Done

- [x] 步骤1: `avformat_find_stream_info` 探测参数调优 (2026-08-08)
  - **实施形态**：`VEDemux.h` 新增 `struct ProbeLimits{probeSizeBytes, analyzeDurationUs, fpsProbeFrames}`
    + `virtual probeLimits()`，本地默认 **512KB / 1s / fpsProbeFrames=0**；
    `VEDemux.cpp` 在 `avformat_find_stream_info` 之前套用
    （`probesize` / `max_analyze_duration` 是结构体字段，
    **`fpsprobesize` 只有 AVOption 没有结构体字段，须 `av_opt_set_int(ctx,"fpsprobesize",...)`**），
    并打一条 ALOGI 记录实际生效值。
  - `VENetworkSource.h` 覆写 `probeLimits()` 返回 `{0,0,-1}` 即**沿用 FFmpeg 默认**
    —— 网络源字节要现拉，探测量不足会漏轨道或拿不到 fps，**那是功能缺陷，
    不是可以用来换启播速度的东西**。
  - **收益**（1920×1080 h264 20Mbps 主素材）：

    | | 解析流信息 | 启播总耗时 |
    |---|---|---|
    | 硬解 优化前 → 后 | 145.0 → **66.1ms**（−54%） | 231.2 → **169.0ms**（−27%） |
    | 软解 优化前 → 后 | 133.8 → **51.1ms**（−62%） | 246.4 → **209.1ms**（−15%） |

    其它素材解析耗时：hevc 0.7ms、无音轨 mp4 2.4ms、纯音频 m4a 1.5ms、mp3 1.0ms。
  - **功能验证通过（本步骤的关键验收，风险 1 正是冲它来的）**：
    - 时长 **9529ms 与 ffprobe 完全一致**；
    - codec 正确识别为 `h264`；
    - 纯音频文件 codec 正确为 `-`、渲染 0 帧、音频后端 AAudio；
    - 渲染帧数 硬解 **278** / 软解 **287**，**与调优前完全一致**
      —— 这**不是回归**：278/287 在调优前就是这两个数字；
    - **fps 无直接读取途径**，间接验证：287 帧 / 9.529s = **30.1fps**，与 30fps 源一致、
      丢帧 0，说明 `VEAVsync` 仍拿到了帧率。
  - **遗留（已决定不再动）**：66ms 仍不算小。512KB 在 20Mbps 下只有约 0.2s 数据，
    而 `find_stream_info` 仍需为每条流解出至少一帧确定像素格式，**这部分是固有成本**。
    继续压 probesize 收益递减而漏轨风险上升，**暂不再动**。

- [x] 步骤2: 修正解码耗时打点（累加器口径，含音频解码器） (2026-08-08)
  - `VEVideoDecoder` / `VEAudioDecoder` 各加成员 `mDecodeAccumUs`：
    `avcodec_send_packet` 前后计时累加；产出一帧时把「累加值 + 本次 receive/转换耗时」
    记进直方图并清零；**`onFlush` 里清零**（否则已送包但未结算的耗时会挂到 flush 后第一帧上）。
  - **修正效果（1080p 软解）**：

    | 指标 | 修正前 | 修正后 |
    |---|---|---|
    | 视频解码 p50 | 0.1ms | **14.0ms**（p95 23.6，max 60.25，n=287） |
    | 音频解码 p50 | 0.05ms | **0.3ms**（p95 0.65，n=445） |

  - **交叉校验（验收 3）通过**：`vdec_thread` 79.5% CPU ÷ 30fps = 每帧约 **26.5ms** CPU，
    与实测 wall p50 14 / p95 23.6 **同量级**。差值来自取包、`VEFrame` 分配等
    落在测量窗口外的部分，以及 FFmpeg 内部多线程解码不全计入 `vdec_thread` 自身。
  - 修正值比原预期 20~30ms 略低但同量级，结论成立。
  - **口径标注要求（风险 3）**：面板/报告须写明「口径已于 2026-08-08 修正，
    与此前 `videoDecodeMs` 数据不可比」。perf-metrics 步骤2 的旧数据已同步更正。

- [x] 步骤3: ALOGV 开/关 CPU 对照实验 (2026-08-08)
  - **结论：假设被证伪。ALOGV 洪水不是 CPU 瓶颈，只值 1.5~4.5 个百分点。**
    "日志占 CPU"这条优化项**从 startup-cpu-opt 中撤回**。
  - 落地的开关（默认 OFF，仅作对照实验工具保留）：
    `utils/Log.h` 条件改为
    `#if defined(VE_QUIET_LOG) || (defined(NDEBUG) && !defined(VE_FORCE_VERBOSE_LOG))`；
    `CMakeLists.txt` 加 `option(VE_QUIET_LOG ... OFF)`；
    `lzplayer_core/build.gradle` 支持 `-PveQuietLog=true` 传下去。
    （实际开关名是 `VE_QUIET_LOG`，与设计稿示例里的 `VE_FORCE_QUIET_LOG` 不同，以代码为准。）
  - 对照实验（1080p，同素材同配置同设备；已确认静音版 `VEDemux` verbose 日志条数为 0，
    即开关确实生效）：

    | | 日志全开 | 静音日志 | 差值 |
    |---|---|---|---|
    | 软解 CPU 单核归一 | 146% | 144.5% | **−1.5pp** |
    | 硬解 CPU 单核归一 | 110.5% | 106.0% | **−4.5pp** |

  - 原推理链（"硬解 110.5% 里主线程只占 23%、其余散在几十个小线程 ⇒ 每线程共有开销 ⇒ 日志"）
    **被证伪**。这正是"先对照实验再改代码"（关键约束 6）挣回来的一次：
    若直接去收敛几十处 ALOGV，会花掉大量工时换 1.5~4.5pp。
  - **日志收敛降级为「可选清理」**，理由只剩一条：这台 OPPO 设备 logcat 有
    300 行/进程配额，洪水会挤掉应用自己的日志（见 perf-metrics 步骤1 教训）。
    **不再是性能项。**

## Doing

- [ ] 步骤4: codec configure 拆分与预热 + 软解首帧不等时钟 + 音频路径 CPU 复查
  — **2026-08-08 第二次重排：零拷贝撤回、纹理预分配收益记 0，子项换成 4a/4b（见下）**

  - **4a（当前最大项，先做）硬解 codec configure 拆分与预热**：configure **61~66ms**，
    占硬解启播 **169ms** 的 **39%**，剩余最大单项。
    - **第一个交付物是拆分数据，不是优化代码**：在 `VEStartupTrace` 增
      `T4A_CODEC_CREATED` / `T4A_CODEC_CONFIGURED`，把 T4a 拆成 **创建 / 配置 / 启动** 三段输出。
    - 三段分别是 `AMediaCodec_createDecoderByType`（组件加载 + binder 到 codec2 服务）、
      `AMediaCodec_configure`、`AMediaCodec_start`。**哪段占大头决定预热要预热什么**：
      - createDecoderByType 占大头 → 播放器构造时后台线程预建常用 mime 的 codec 实例，configure 时复用；
      - configure 占大头 → 预热无效，改为**与 `find_stream_info` 并行**
        （mp4 在 `avformat_open_input` 后 streams 已带 codec_id/width/height/extradata(avcC)，
        可让两个 66ms 变成一个）；该路改动大，需 demux 在 open_input 后就暴露轨道参数。
    - 反模式仍有效：把 configure 挪进 prepare 等它完成**不算优化**，只是搬时间。
    - 验收：三段拆分与现有 T4a 区间自洽；优化后看**启播总耗时**。

    **4a 工作区现状（2026-08-08 核对，均为未提交改动）—— 拆分已出数，方向已由数据选定：**
    - 拆分打点已落地：`VEStartupTrace.h` 增 `T4A_CODEC_CREATED` / `T4A_CODEC_CONFIGURED`，
      `VEStartupTrace.cpp` 输出 `codecCreateMs` / `codecConfigureMs` / `codecStartMs`
      并把 `marks` 扩为 `t4aCreated` / `t4aConfigured`；
      打点埋在 `VEMediaCodecVideoDecoder.cpp` 的 `createDecoderByType` 与
      `AMediaCodec_configure` 之后。**软解不填这两个点。**
    - **拆分结论（据工作区代码注释记录的实测值）**：硬解 configure 总 **64~87ms** 中
      `AMediaCodec_createDecoderByType` 独占 **50~66ms（76~78%）**，
      `AMediaCodec_configure` 只占 **5.6~7.7ms**。
      → **命中"createDecoderByType 占大头"分支**；
      **"configure 与 `find_stream_info` 并行"那条大改动因此不必做**（它只值 5.6~7.7ms）。
    - 据此实施的优化：新增 `platform/android/decoders/VECodecWarmup.{h,cpp}`（未跟踪文件）——
      在 `VEDemux` 的 `avformat_open_input` 返回后（约 5ms 处、`codec_id` 已知，**不是猜 mime**）
      按 codec_id 在后台线程预建 codec 实例，**完整藏进随后 `find_stream_info` 的 51~66ms 窗口**；
      解码器侧 `take(mime)` 优先取预热实例、取不到照常自建（预热是"赶上就赶上"，不作正确性依赖）；
      `VEPlayer::onRelease` 调 `discard()` 兜底，避免无人取用的实例长期占着有限系统资源。
      跳过内嵌封面流（`AV_DISPOSITION_ATTACHED_PIC`）。
    - **待办**：真机验证**硬解启播总耗时**的实际下降（当前 169.0ms），
      以及"预热未被取用"路径（非硬解白名单 / 回退软解 / prepare 失败）不泄漏 codec 实例；
      改动尚未提交。

  - **4b（新增）软解首帧不等同步时钟，立即上屏**：那 57ms 等待可以靠
    「首帧不等同步时钟、立即上屏」消掉 —— 常规做法是**先把第一帧画出来，再启动时钟对齐**。
    直接压缩感知延迟，**改动比零拷贝小**。
    风险：首帧可能比音频略早显示，需确认**无可见跳动**。
    验收：软解 T6→T7 显著下降**且启播总耗时同步下降**。

  - **4c（保留原位）音频路径 CPU 复查**：21~26% 单核 CPU，见下方「后续议题」。

  - **动手前须确认 vulkan-renderer 工作区状态** —— 4a 已不碰渲染文件，
    但 **4b 会碰 `VEVideoDisplay` / `VEAVsync`**（其步骤7 真机验证仍被锁屏问题阻塞、改动仍在工作区）。

### 步骤4 已撤回/降级的两个子项（2026-08-08 实测，保留追溯）

1. **~~4a-旧 软解零拷贝上屏~~ → 撤回，不做。实测否掉了它。**
   `present` p50 4.4ms 的三段构成：

   | 段 | p50 | p95 |
   |---|---|---|
   | 纹理上传 | **1.8ms** | 4.1ms |
   | 绘制 | 0.3ms | — |
   | `eglSwapBuffers` | 1.8ms | 3.0ms |

   零拷贝只能省"上传"那 1.8ms → 30fps 下单核 **5.4%**，在软解总 CPU **144.5%** 里占 **3.7%**。
   代价：`AHardwareBuffer_lockPlanes` 要 **API 29**（minSdk 24）、
   YUV→RGB 交给驱动会**丢掉已修好的 BT.601/709 × full/limited 控制**、
   需要 FFmpeg 自定义 `get_buffer2` + 缓冲池 + EGLImage 生命周期，且仍需保留回退路径。
   **投入产出不成立。**
   **顺带更正一处早先归因**：`video_render` 27% CPU 里只有约 **13%** 在 `renderFrame` 内
   （4.4ms × 30fps），另一半在 **AVSync 调度、消息循环、帧队列**上。
   把 27% 整个归给"三平面上传"是不准的。

2. **纹理预分配 → 已实施并提交，但不计入收益（记 0）。**
   GLES 纹理改为 prepare 阶段按容器声明尺寸预分配。但**没有改善目标读数**：
   首帧上屏三次实测 **51.7 / 98.2 / 58.8ms**，与预分配前同量级且方差很大。
   原因是**又一次搞错了段落含义** —— T6→T7 量的是「首帧解出 → 提交上屏」，
   **中间包含 AVSync 的等待**，软解带音频时视频首帧在等音频时钟起锚（首声 38.1ms 就在这个量级）。
   **铁证**：同一份数据里 `present` 的 **max 只有 9.98ms**，`renderFrame()` 从来没花过 57ms。
   预分配本身无害（确实省掉首帧的 `glTexStorage2D`×3），**代码保留，收益记 0**。
   真正该做的事因此变成 **4b**。

### 本 feature 第三次同类错误（2026-08-08 记录）

本轮在**"先假设某个数字代表什么、再照着它去优化"**上连续栽三次，**三次都是靠先做拆分测量才拦住**：

| 次 | 读数 | 当时以为 | 拆分后真相 |
|---|---|---|---|
| 1 | 硬解 `videoDecodeMs` | 解码耗时 | pts→出队端到端延迟，绝大部分是背压等待 |
| 2 | 软解 `videoDecodeMs` | 解码耗时 | 只覆盖 receive 侧，不含 `avcodec_send_packet` |
| 3 | 软解「首帧上屏 57.2ms」 | 首次纹理分配 | 含 AVSync 等音频时钟起锚；`present` max 仅 9.98ms |

已沉淀规则"新指标必须有独立来源的交叉校验"**没能防住第 3 次**
（采集点与数字都没错，错在**读法**：把复合区间当成单一操作的耗时）。
**因此追加一条规则：动手优化某个读数之前，先确认它的构成，不能只看它的名字。**
（已写入 plan 关键约束 8 与设计文档 §0 第 4 条 / §0.1。）

## Todo

（步骤1~3 已完成；步骤4 三个子项 4a/4b/4c 的进展在上方 Doing 段内跟踪，
不再拆成独立步骤，以免总表完成度口径变动。）

## 步骤1~3 完成后的实测基线（1080p，静音日志版，步骤4 的依据）

启播（对比 §1 优化前基线：硬解 231.2 / 软解 246.4）：

| | 硬解 | 软解 |
|---|---|---|
| 解析流信息 | 66.1（原 145.0） | 51.1（原 133.8） |
| **启播总耗时** | **169.0（原 231.2，−27%）** | **209.1（原 246.4，−15%）** |
| configure | 61~66 | — |
| 首帧上屏 | 0.7 | 57.2（~~首次纹理分配~~ → **实为 AVSync 等音频时钟起锚**，见步骤4 第 2 条） |
| 上屏 p50 / p95 | 1.5 / 2.7 | 5.7 / 8.2 |

静音日志后的 CPU 构成：

| 路径 | 总计 | 构成 |
|---|---|---|
| 软解 | **144.5%** | `vdec_thread` **79.5%**（FFmpeg 1080p30 h264 真解码，固有成本）+ `video_render` **27.0%**（GLES 三平面上传）+ 音频路径约 **21%**（AudioTrack 9.5 / audio_render 6.0 / adec 5.5） |
| 硬解 | **106.0%** | `vdec_thread` 24% + NDK MediaCodec 14.5% + 音频路径约 **26%**（AudioTrack 11.5 / audio_render 8.0 / adec 6.5）+ HwBinder 7% |

硬解比软解省 **38.5pp**，几乎全部省在解码本身（79.5 → 24）。

## 后续议题：音频路径 CPU 偏高（2026-08-08 新观察，已列为步骤4c）

**音频路径在软/硬解两条路上都占 21~26% 单核 CPU**，对 **AAC 立体声 44.1k** 而言偏高，
且 **1.0x 速率下不该有 sonic 变速开销**。构成：
`audio_render` 6~8% + `adec` 5.5~6.5% + 系统 `AudioTrack` 9.5~11.5%。
**值得单独查一次**，排在 4a / 4b 之后。

## 优化前基线（本 feature 唯一对照基准，详见设计文档 §1）

环境：OPPO PHK110 / Android 16 / ColorOS，8 核，Debug 构建（ALOGV 全开）。
素材：`/sdcard/Movies/VID_20230814_205835.mp4` = 1920×1080 h264 30fps 20Mbps 9.5s + AAC。

| 指标 | 硬解 | 软解 |
|---|---|---|
| 启播总耗时 | 231.2ms | 246.4ms |
| 打开容器 | 5.2 | 5.0 |
| **解析流信息** | **145.0** | **133.8** |
| 建链 | 0.7 | 0.7 |
| configure | 66.3（叠加在首帧窗口内） | 0.5 |
| 首帧解码 | 79.6 | 49.6 |
| 首帧上屏 | 0.7 | 57.2 |
| 播放中 CPU（单核归一） | 110.5%（整机 13.81%） | 146%（整机 18.25%） |
| 上屏 p50 / p95 | 1.5 / 2.7ms | 5.3 / 8.5ms |
| 同步余量 p50 / p95 | 12.0 / 32.5ms | 5.0 / 29.0ms |
| 丢帧 | 0 / 278 | 0 / 287 |

线程 CPU：软解 `vdec_thread` 81%、`video_render` 27.5%；
硬解 `vdec_thread` 23%、NDK MediaCodec 10.5%、HwBinder 7%。

**注意 2（2026-08-08 追加）**：表中软解「首帧上屏 57.2ms」**不是渲染耗时** ——
T6→T7 含 AVSync 等音频时钟起锚，`present` max 仅 9.98ms。该数字不可用于判定渲染优化收益。

**注意**：表中软解 `videoDecodeMs`（perf-metrics 报 p50=0.1ms）**是错的**，
口径缺陷已在步骤2 修完，**修正后实测 p50 = 14.0ms**（原预期 20~30ms，实际略低但同量级）。
基线里其余数字不受该缺陷影响。

## 触发背景：1080p 真实素材推翻了小素材上的结论

perf-metrics 全部测量此前用的是 ffmpeg 造的 **640×360** 合成片。换 1080p 真实素材后瓶颈排序完全变了：

| | 640×360 合成片 | 1920×1080 真实素材 | 变化 |
|---|---|---|---|
| 解析流信息 | 2.4 / 2.1ms | **145.0 / 133.8ms** | 约 60 倍，从可忽略项跃升为**启播第一大项** |
| 启播总耗时（硬解） | 101.7ms | 231.2ms | 2.3 倍 |
| 启播总耗时（软解） | 11.8ms | 246.4ms | 20.9 倍；软硬解从"差 8.6 倍"变成**基本持平** |

旧结论"硬解起播比软解慢 8.6 倍、瓶颈是 MediaCodec configure"是那份数据的正确读法，
但它是**素材的性质**而不是播放器的性质。**解析流信息这个瓶颈在小素材上完全不可见。**

方法论教训（已写进设计文档 §0 与 plan 关键约束 1/2）：
性能结论必须用有代表性的素材得出；合成小素材会**系统性单向掩盖**真实瓶颈；
换素材必须重测全部结论 —— 本次变的不是一个数字，是排序。

## 已撤回/降级的三条结论（2026-08-08）

1. **「缓冲按包数封顶」不成立 —— 从优化建议中撤回**。1080p 20Mbps 视频包峰值仅 **64~71**，
   640×360 是 **216**；若按包数设限高码率应同样撞 216。说明上限本来就按字节/时长算
   （16MB 全局字节封顶 + 每路时长达标），高码率下更早触及字节封顶。是设计意图，不是缺陷。
   已同步标注进 perf-metrics 的 status。
2. **「RSS 254MB」降级为待测 —— 不是问题**。该值测于 640×360 且含大量共享库映射；
   1080p 下未单独测内存。要判断需专门测一轮（RSS/PSS/native heap 三口径 +
   播放前后差分），是否立项等数据。已同步标注进 perf-metrics 的 status。
3. **「ALOGV 洪水是 CPU 底噪主因」—— 步骤3 对照实验证伪，撤回**（软解 −1.5pp、硬解 −4.5pp）。
   日志收敛降级为可选清理，理由只剩 logcat 300 行配额。
   perf-metrics status 里那条「建议起日志分级收敛任务」的性能理由已同步标注为已证伪。

## 顺带更新的设计稿（2026-08-08）

`docs/test-console-ui-design.md`：
- **§3.C2 瀑布图示例数字已替换为真实 1080p 数据**（原为早期凭空构造的
  打开 118 / 解析 42 / 建链 386 / configure 341 / 首帧 264、总 842ms，与真机差距很大且会误导）。
  已保留说明：示例数字来自 OPPO PHK110 + 1920×1080 h264 20Mbps，换设备或素材会有显著差异。
- **§5 采集点表中的 640×360 旧数据已标注**为"合成小素材，仅用于验证采集点完整性，
  不可作为性能基线"。

`docs/startup-cpu-opt-design.md`（2026-08-08 追加）：
- §2 步骤3 增「实测结论：假设被证伪」小节；步骤4 标题去掉"优先级待定"并写入定序表。
- §3 涉及文件表按实际落地更新（`ProbeLimits` 结构体、音频解码器一并修、`VE_QUIET_LOG`
  开关名、分级收敛标为不做）。
- 新增 **§7 实施结论（步骤1~3）**：7.1 探测调优收益与功能验证、7.2 打点修正与交叉校验、
  7.3 证伪结论、7.4 静音日志后的 CPU 构成、7.5 音频路径 CPU 偏高的新观察。
