<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--
      Android 13(API 33) 起 READ_EXTERNAL_STORAGE 对媒体文件失效，被拆成三个
      分类型权限。少声明 READ_MEDIA_AUDIO 的后果不是"看不到音频文件"，而是
      纯音频文件连 open 都会被拒(avformat_open_input 拿到 EACCES)，表现为
      "demux open failed"，很容易误判成解封装能力缺陷。
      IMAGES/VIDEO 由 MediaSelector 模块声明并合并进来，这里补上 AUDIO。
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <!-- 网络源测试需要 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Lzplayer"
        tools:targetApi="31">
        <!-- 测试台：现在的入口。旧的 MainActivity 保留作对照，从桌面拿不到，
             需要时用 adb am start 直接拉起。 -->
        <activity
            android:name=".console.ConsoleActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.LZConsole"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:screenOrientation="fullUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"/>

    </application>

</manifest>